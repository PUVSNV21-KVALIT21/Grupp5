@page
@model hakimlivs.Pages.CartItems.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>CartItem</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CartItem.Ammount)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CartItem.Ammount)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.CartItem.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
